import type { WorkspaceEntry } from '../../api/workspace-store.js'

export function LocalTab(_props: { onAdd: (e: WorkspaceEntry) => void; existingIds?: string[] }) {
  return <div>Local tab (coming soon)</div>
}
